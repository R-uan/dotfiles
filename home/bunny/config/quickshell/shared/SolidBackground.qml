import QtQuick
import qs.shared
import QtQuick.Window

Rectangle {
  border.width: 2
  width: layout.width
  radius: Theme.radius
  height: parent.height
  color: Theme.background
  border.color: Theme.borderColour
}
